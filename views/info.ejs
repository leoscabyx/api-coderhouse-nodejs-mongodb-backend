<!DOCTYPE html>
<html lang="es">
    <%- include("./partials/head") %>
<body>
    <%- include("./partials/header") %>
    <main>
        <section class="seccion-info">
            <div class="detalle">
                <h2 class="detalle__title">Detalle del Servidor en Node</h2>
                <ul class="detalle__list">
                    <% if (data) { %>
                        <li><span class="detalle__term">Argumentos:</span> <%= data.argumentos %></li>
                        <li><span class="detalle__term">Plataforma:</span> <%= data.plataforma %></li>
                        <li><span class="detalle__term">Version Node:</span> <%= data.versionNode %></li>
                        <li><span class="detalle__term">Memoria Total:</span> <%= data.memoriaTotal %></li>
                        <li><span class="detalle__term">Carpeta del Proyecto:</span> <%= data.carpetaProyecto %></li>
                        <li><span class="detalle__term">Path de Ejecucion:</span> <%= data.pathEjecucion %></li>
                        <li><span class="detalle__term">Cpu's:</span> <%= data.cpus %></li>
                        <li><span class="detalle__term">Processo ID:</span> <%= data.pid %></li>
                    <% }else{ %>
                        <li>No se puede mostrar tu perfil</li>
                    <% } %> 
                </ul>
            </div>            
        </section>
    </main>
</body>
</html>